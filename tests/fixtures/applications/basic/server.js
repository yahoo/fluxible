//var path = require('path');
//var expressInstance = require('express')();
//var app = require('./app');
//var renderServer  = require('../../../../utils/renderServer');
//
//expressInstance.use(express['static'](path.join(__dirname, '/build')));
//expressInstance.use(require('body-parser')());
//expressInstance.use(app.getXHRPath(), app.getFetcherClass().middleware());
//expressInstance.use(require('../../../../utils/middleware'));
