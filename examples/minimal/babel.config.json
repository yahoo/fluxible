{
  "presets": ["@babel/preset-env", "@babel/preset-react"],
  "env": {
    "browser": {
      "presets": [
        [
          "@babel/preset-env",
          {
            "targets": "defaults"
          }
        ]
      ]
    },
    "server": {
      "presets": [
        [
          "@babel/preset-env",
          {
            "targets": { "node": "current" }
          }
        ]
      ]
    }
  }
}
